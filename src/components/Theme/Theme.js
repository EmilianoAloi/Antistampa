import { createTheme } from "@mui/material"

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#FFFF00',
        },
        success: {
            main: '#02FF01',
        },

     

        background: {
            default: '#000000',
            paper: '#000000',
        },
    },
});

