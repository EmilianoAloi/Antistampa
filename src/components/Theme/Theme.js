import { createTheme } from "@mui/material"

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#F4BF00',
        },
        secondary: {
            main: '#F4BF00',
        },
        background: {
            default: '#000000',
            paper: '#000000',
        },
    },
});

